Edge functions — noms de secrets à définir sur CHAQUE nouveau projet Supabase (develop / staging / production).

Refs actuels (compte perso) :
  develop   = ojtrsrdcaugyzvtqtupf
  staging   = giguuzfnjkkqdeteujwc
  production = aueuxlqtueoqjxsdemeu

fixa-pro-hub (confirmé via `supabase secrets list --project-ref`) :
  N8N_WEBHOOK_URL
  ORDER_UPDATE_API_KEY
  RESEND_API_KEY
  STRIPE_SECRET_KEY
  STRIPE_WEBHOOK_SECRET
  SUPABASE_ANON_KEY
  SUPABASE_DB_URL
  SUPABASE_SERVICE_ROLE_KEY
  SUPABASE_URL
  VAB_API_KEY

admin-hub-central (d’après le code Deno.env) :
  SUPABASE_URL
  SUPABASE_ANON_KEY
  SUPABASE_SERVICE_ROLE_KEY
  SUPABASE_DB_URL   (manage-categories)
  RESEND_API_KEY    (send-order-email)
  VAB_API_KEY       (update-order-status)

Après création des projets 3K : supabase link --project-ref <nouveau> puis supabase secrets set KEY=value pour chaque clé.
