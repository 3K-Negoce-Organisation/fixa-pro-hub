-- Projet fusionné fixa + admin : le schéma de base est déjà appliqué par les migrations storefront.
-- L’ancienne capture « remote_schema » entrait en conflit (drop function / triggers) ; no-op volontaire.
SELECT 1;
